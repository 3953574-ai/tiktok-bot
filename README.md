# 🇺🇦 TikTok & Twitter (X) Downloader Bot

Цей Telegram бот дозволяє автоматично завантажувати відео та фото з **TikTok** та **Twitter (X)** у найкращій якості та **без водяних знаків**.

Бот автоматично розпізнає посилання, завантажує медіа-контент (відео, фото, слайд-шоу), витягує аудіо та перекладає описи постів українською мовою.

---

## ✨ Особливості

*   **TikTok:**
    *   Завантаження відео без водяного знаку (Full HD).
    *   Підтримка фото-слайдерів (завантажує як галерею).
    *   Можливість витягнути аудіо-трек.
*   **Twitter (X):**
    *   Завантаження відео та GIF.
    *   Завантаження фото та галерей.
*   **Smart Features:**
    *   🌍 **Автопереклад:** Описи іноземними мовами (крім англійської) автоматично перекладаються на українську.
    *   🔗 **Посилання:** Додає клікабельні посилання на автора та оригінальний пост.
    *   🎧 **Режими:** Гнучка система команд для отримання чистого контенту або музики.

---

## 🤖 Як користуватися ботом

Просто надішли посилання на відео з TikTok або пост у Twitter (X). Бот підтримує спеціальні команди, які можна додавати до посилання в тому ж повідомленні.

### 1️⃣ Стандартний режим
Просто надішли посилання.
> **Отримаєш:** Відео/Фото + Гарний підпис з перекладом + Посилання на автора.

### 2️⃣ 🎵 Режим "Меломан"
Надішли посилання і допиши `!a` (або `audio`, `music`).
> **Отримаєш:** Відео + Окремий MP3 файл.
> *(Примітка: Для фото-слайдерів TikTok музика додається автоматично).*

### 3️⃣ 🤫 Чистий режим
Надішли посилання і допиши знак мінус `-` (або `!`, `clear`).
> **Отримаєш:** Тільки файл відео/фото без жодного тексту, підписів та перекладів.

### 4️⃣ Комбо-режим
Можна поєднувати команди. Наприклад: `посилання - !a`
> **Отримаєш:** Чисте відео без тексту + MP3 файл.

---

## 🛠 Технічний стек

*   **Python 3.9+**
*   [aiogram 3.x](https://github.com/aiogram/aiogram) — Асинхронний фреймворк для Telegram ботів.
*   [aiohttp](https://github.com/aio-libs/aiohttp) — Асинхронні HTTP запити.
*   [deep-translator](https://github.com/nidhaloff/deep-translator) — Переклад тексту.
*   [langdetect](https://github.com/Mimino666/langdetect) — Визначення мови тексту.

---

## 🚀 Встановлення та запуск (Локально)

1.  **Клонуйте репозиторій:**
    ```bash
    git clone https://github.com/your-username/your-repo.git
    cd your-repo
    ```

2.  **Створіть віртуальне середовище (рекомендовано):**
    ```bash
    python -m venv venv
    # Windows
    venv\Scripts\activate
    # macOS/Linux
    source venv/bin/activate
    ```

3.  **Встановіть залежності:**
    ```bash
    pip install -r requirements.txt
    ```

4.  **Налаштування змінних оточення:**
    Створіть файл `.env` (або просто експортуйте змінну в терміналі):
    ```env
    BOT_TOKEN=ващ_токен_від_botfather
    ```

5.  **Запуск бота:**
    ```bash
    python main.py
    ```

---

## ☁️ Деплой на Render.com

Цей бот налаштований для безкоштовного хостингу на **Render**. У коді реалізовано веб-сервер (`aiohttp web`), щоб Render не присипляв бота.

1.  Зареєструйтесь на Render.
2.  Створіть новий **Web Service**.
3.  Підключіть ваш GitHub репозиторій.
4.  Вкажіть:
    *   **Runtime:** Python 3
    *   **Build Command:** `pip install -r requirements.txt`
    *   **Start Command:** `python main.py`
5.  У розділі **Environment Variables** додайте:
    *   `BOT_TOKEN`: ваш токен бота.
    *   `PORT`: `8080` (або інший, але код автоматично бере порт з ENV).

---

### 📝 Примітки
*   Бот використовує публічні API (TikWM, FxTwitter) для отримання посилань.
*   Код включає обробку помилок та логування.